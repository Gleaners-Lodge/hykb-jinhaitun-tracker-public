# 项目开发说明

这是 Python 标准库采集器与零依赖静态看板，已归档。目录、数据来源、运行方法与部署变量见 README.md。

- 默认保留历史数据，不启用定时采集。改采集或部署配置时维持归档保护，除非任务明确要求恢复。
- 快照是源数据，聚合文件由脚本重建。活动内部 id 与主站 gid 不可混用。
- 活动公开查询使用 POST；不访问投票、投票记录或其他需登录接口。
- 维护请求间隔和并发限制；不要增加源站负担。
- 本地预览使用 `./serve.sh`，网页需要 HTTP 加载 JSON。
- `FOCUS_ID` 和 `PROMO` 默认留空，避免绑定特定参赛者。
- 图表色槽数量须与 CSS 一致；不要给 body 设置固定的 100% 高度，以免破坏 sticky。
- 提交前检查个人信息和凭据，不添加个人部署目标或本地绝对路径。
- 中文注释和提交说明；不加 AI 署名 trailer。
